<div id="serverTime">
    @DateTime.Now.ToString("dd.MM.yyyy HH:mm:ss")

    @Ajax.ActionLink(
        "Refresh",
        "GetServerTime",
        "Courses",
        null,
        new AjaxOptions()
        {
            UpdateTargetId = "serverTime",
            InsertionMode = InsertionMode.Replace,
            HttpMethod = "GET"
        },
        new { id = "anchorGetServerTime" }
        )
</div>
